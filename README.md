# UI Controls based on react

## Overview

This repository is having UI controls built on top of react framework. The controls are designed in such a way that its easy to use them in any buisness application.
The main goal of these controls will be data driven. 